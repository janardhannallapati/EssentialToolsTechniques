To view the contents of a file with line numbers 
>cat -n /tmp/hello
or 
nl /tmp/hello


To view only the first x lines from the beginning of a file.
>head -n1 /tmp/hello
>head -n10 /tmp/hello

To view only a specific number of bytes (characters) from the beginning of a file
>head -c1 /tmp/hello

To view only the last x files from the end of a file.
>tail -n1 /tmp/hello

t
